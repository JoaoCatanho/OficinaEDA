#include "Inicialização.h"
#include <iostream>

/*using namespace std;

struct oficina;

/*void inicializar_ETs(struct ETs* ets, int n_ets) {
    for(int i = 0; i < n_ets; i++) {
        ets[i].capacidadeCarros = rand() % 4 + 2; // Capacidade aleatória entre 2 e 5
        ets[i].capacidadeCarros = rand() % ets[i].capacidadeCarros + 1; // Conteúdo aleatório entre 1 e a capacidade
    }
}





int numETs(int numets){
    int tamanho=5;
    for (int i=0;i<tamanho; i++){
        cout<<"Insira o numero de ETS:\n"<<endl;
        cin>> numets;
    }
    return 0;
}
*/









